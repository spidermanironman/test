# 广播发送全流程详解

## 概述

本文档详细解释广播（Broadcast）发送的完整流程，以Android系统为例，涵盖从应用层调用到底层Binder通信的整个链路。

## 一、应用层调用

### 1.1 发送广播的入口

在Android应用中，发送广播通常通过以下方式：

```java
// 普通广播
Intent intent = new Intent("com.example.ACTION_BROADCAST");
intent.putExtra("key", "value");
context.sendBroadcast(intent);

// 有序广播
context.sendOrderedBroadcast(intent, null);

// 带权限的广播
context.sendBroadcast(intent, "com.example.permission");
```

### 1.2 ContextWrapper.sendBroadcast()

应用调用`sendBroadcast()`时，实际调用链如下：

```
Context.sendBroadcast()
  └─> ContextWrapper.sendBroadcast()
      └─> ContextImpl.sendBroadcast()
```

## 二、系统服务层（ActivityManagerService）

### 2.1 ContextImpl.sendBroadcast()

`ContextImpl`是Context的实现类，负责将广播请求转发给系统服务：

```java
public void sendBroadcast(Intent intent) {
    // 1. 验证Intent的有效性
    // 2. 获取ActivityManagerService的代理对象
    ActivityManager.getService().broadcastIntent(...);
}
```

### 2.2 ActivityManagerService.broadcastIntent()

这是广播发送的核心方法，主要流程包括：

#### 2.2.1 权限和Intent验证
- 检查发送者权限
- 验证Intent的合法性
- 处理Intent的flags（如FLAG_RECEIVER_REGISTERED_ONLY）

#### 2.2.2 查询匹配的接收者
```java
// 查询所有注册的BroadcastReceiver
List<ResolveInfo> receivers = queryIntentReceivers(intent, ...);
```

系统会查询：
- 静态注册的接收者（AndroidManifest.xml中声明）
- 动态注册的接收者（通过registerReceiver注册）

#### 2.2.3 过滤和排序
- 根据IntentFilter匹配接收者
- 对于有序广播，按priority排序
- 过滤掉不符合条件的接收者

## 三、BroadcastQueue处理

### 3.1 广播队列分类

Android系统维护多个广播队列：

1. **前台队列（Foreground Queue）**
   - 高优先级广播
   - 超时时间：10秒

2. **后台队列（Background Queue）**
   - 普通广播
   - 超时时间：60秒

3. **系统队列（System Queue）**
   - 系统级广播

### 3.2 BroadcastRecord创建

系统为每个广播创建`BroadcastRecord`对象：

```java
BroadcastRecord r = new BroadcastRecord(
    queue, intent, callerApp, ...);
```

`BroadcastRecord`包含：
- Intent信息
- 接收者列表
- 发送者信息
- 权限信息
- 状态信息

### 3.3 入队操作

```java
queue.enqueueOrderedBroadcastLocked(r);
// 或
queue.enqueueParallelBroadcastLocked(r);
```

## 四、广播分发流程

### 4.1 BroadcastQueue调度

`BroadcastQueue`通过`scheduleBroadcastsLocked()`调度广播分发：

```java
void scheduleBroadcastsLocked() {
    if (mBroadcastsScheduled) {
        return; // 已经调度，避免重复
    }
    mHandler.sendMessage(...);
    mBroadcastsScheduled = true;
}
```

### 4.2 处理广播消息

`BroadcastHandler`处理广播分发消息：

```java
case BROADCAST_INTENT_MSG:
    processNextBroadcast(true);
    break;
```

### 4.3 processNextBroadcast()

这是广播分发的核心方法，主要步骤：

#### 4.3.1 处理并行广播（Parallel Broadcasts）

```java
// 1. 遍历并行广播列表
while (mParallelBroadcasts.size() > 0) {
    BroadcastRecord r = mParallelBroadcasts.remove(0);
    
    // 2. 遍历所有接收者
    for (int i = 0; i < N; i++) {
        Object target = r.receivers.get(i);
        
        // 3. 分发到接收者
        deliverToRegisteredReceiverLocked(r, target, ...);
    }
}
```

#### 4.3.2 处理有序广播（Ordered Broadcasts）

```java
// 1. 获取当前广播
BroadcastRecord r = mOrderedBroadcasts.get(0);

// 2. 查找下一个接收者
while (r.nextReceiver < r.receivers.size()) {
    Object nextReceiver = r.receivers.get(r.nextReceiver);
    
    // 3. 分发到接收者
    deliverToRegisteredReceiverLocked(r, nextReceiver, ...);
    
    // 4. 等待接收者处理完成（异步）
    r.nextReceiver++;
    return; // 等待onReceive完成
}
```

## 五、接收者分发

### 5.1 deliverToRegisteredReceiverLocked()

此方法负责将广播分发给具体的接收者：

```java
void deliverToRegisteredReceiverLocked(
    BroadcastRecord r, Object target, ...) {
    
    // 1. 获取接收者信息
    BroadcastFilter filter = (BroadcastFilter) target;
    IIntentReceiver receiver = filter.receiverList.receiver;
    
    // 2. 权限检查
    if (!checkComponentPermission(...)) {
        return;
    }
    
    // 3. 发送到接收者进程
    performReceiveLocked(filter.receiverList.app, ...);
}
```

### 5.2 进程间通信（Binder）

#### 5.2.1 获取目标进程

```java
ProcessRecord app = filter.receiverList.app;
```

#### 5.2.2 检查进程状态

- 如果进程未启动，需要启动进程
- 如果进程已死亡，跳过该接收者
- 如果进程正在启动，延迟分发

#### 5.2.3 Binder调用

```java
// 通过Binder IPC调用接收者的onReceive方法
app.thread.scheduleReceiver(
    intent, resultReceiver, ...);
```

## 六、Binder通信层

### 6.1 ApplicationThread.scheduleReceiver()

这是Binder服务端方法，在目标进程的Binder线程中执行：

```java
public final void scheduleReceiver(Intent intent, ...) {
    ReceiverData r = new ReceiverData(intent, ...);
    sendMessage(H.RECEIVER, r);
}
```

### 6.2 ActivityThread处理

`ActivityThread`的`H` Handler处理`RECEIVER`消息：

```java
case RECEIVER:
    handleReceiver((ReceiverData) msg.obj);
    break;
```

### 6.3 handleReceiver()

```java
void handleReceiver(ReceiverData data) {
    // 1. 创建Receiver实例
    BroadcastReceiver receiver = getReceiver(data.intent);
    
    // 2. 创建Context
    Context context = getReceiverContext(data.intent);
    
    // 3. 调用onReceive
    receiver.onReceive(context, data.intent);
    
    // 4. 通知AMS处理完成
    ActivityManager.getService().finishReceiver(...);
}
```

## 七、接收者处理

### 7.1 BroadcastReceiver.onReceive()

应用层接收者实现此方法：

```java
public class MyReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        // 处理广播
        String action = intent.getAction();
        // ...
    }
}
```

### 7.2 处理时间限制

- **前台广播**：必须在10秒内完成
- **后台广播**：必须在60秒内完成
- 超时会导致ANR（Application Not Responding）

### 7.3 异步处理

如果处理耗时，应该启动Service或线程：

```java
@Override
public void onReceive(Context context, Intent intent) {
    // 启动Service处理耗时操作
    Intent serviceIntent = new Intent(context, MyService.class);
    context.startService(serviceIntent);
    
    // 或使用goAsync()
    final PendingResult pendingResult = goAsync();
    new Thread(() -> {
        // 耗时操作
        pendingResult.finish();
    }).start();
}
```

## 八、完成通知

### 8.1 finishReceiver()

接收者处理完成后，通知AMS：

```java
ActivityManager.getService().finishReceiver(
    token, resultCode, resultData, ...);
```

### 8.2 继续下一个接收者

对于有序广播，AMS继续处理下一个接收者：

```java
// 在finishReceiver中
if (r.nextReceiver < r.receivers.size()) {
    processNextBroadcast(false); // 继续处理
} else {
    // 所有接收者处理完成
    r.done = true;
}
```

### 8.3 最终结果处理

如果是有序广播且设置了最终接收者：

```java
if (r.resultTo != null) {
    // 发送最终结果给发送者
    performReceiveLocked(...);
}
```

## 九、流程图总结

```
应用层
  │
  ├─> Context.sendBroadcast()
  │
系统服务层
  │
  ├─> ContextImpl.sendBroadcast()
  │
  ├─> ActivityManagerService.broadcastIntent()
  │     ├─> 权限验证
  │     ├─> 查询接收者
  │     └─> 创建BroadcastRecord
  │
  ├─> BroadcastQueue.enqueueBroadcastLocked()
  │
  ├─> BroadcastQueue.scheduleBroadcastsLocked()
  │
  ├─> BroadcastQueue.processNextBroadcast()
  │     ├─> 并行广播：同时分发给所有接收者
  │     └─> 有序广播：逐个分发
  │
  ├─> deliverToRegisteredReceiverLocked()
  │     ├─> 权限检查
  │     ├─> 进程检查
  │     └─> Binder IPC调用
  │
Binder通信层
  │
  ├─> ApplicationThread.scheduleReceiver()
  │
  ├─> ActivityThread.handleReceiver()
  │
  └─> BroadcastReceiver.onReceive()
  │
  ├─> ActivityManagerService.finishReceiver()
  │
  └─> 继续下一个接收者（有序广播）
```

## 十、关键组件说明

### 10.1 BroadcastRecord

广播记录对象，包含：
- `intent`: 广播Intent
- `receivers`: 接收者列表
- `nextReceiver`: 当前处理的接收者索引
- `resultTo`: 最终结果接收者
- `resultCode`: 结果码
- `resultData`: 结果数据

### 10.2 BroadcastFilter

接收者过滤器，包含：
- `receiverList`: 接收者列表
- `requiredPermission`: 所需权限
- `actionFilter`: Action过滤器

### 10.3 BroadcastQueue

广播队列，管理：
- `mParallelBroadcasts`: 并行广播列表
- `mOrderedBroadcasts`: 有序广播列表
- `mPendingBroadcast`: 待处理的广播

## 十一、性能优化要点

### 11.1 避免在onReceive中执行耗时操作

```java
// ❌ 错误做法
@Override
public void onReceive(Context context, Intent intent) {
    // 耗时操作会导致ANR
    processLargeData();
}

// ✅ 正确做法
@Override
public void onReceive(Context context, Intent intent) {
    Intent serviceIntent = new Intent(context, ProcessService.class);
    context.startService(serviceIntent);
}
```

### 11.2 使用goAsync()异步处理

```java
@Override
public void onReceive(Context context, Intent intent) {
    final PendingResult pendingResult = goAsync();
    AsyncTask.execute(() -> {
        // 异步处理
        processData();
        pendingResult.finish();
    });
}
```

### 11.3 合理使用有序广播

- 有序广播会逐个处理，影响性能
- 只在需要控制处理顺序时使用
- 普通广播使用并行分发

## 十二、常见问题

### 12.1 广播丢失

**原因**：
- 接收者进程未启动
- 权限不足
- IntentFilter不匹配

**解决**：
- 使用静态注册（AndroidManifest.xml）
- 检查权限配置
- 验证IntentFilter

### 12.2 ANR问题

**原因**：
- onReceive执行时间过长
- 主线程阻塞

**解决**：
- 使用Service处理耗时操作
- 使用goAsync()异步处理

### 12.3 广播顺序问题

**原因**：
- 并行广播无序执行
- 有序广播priority设置不当

**解决**：
- 使用有序广播
- 正确设置priority值

## 十三、调试技巧

### 13.1 查看广播日志

```bash
adb logcat | grep -i broadcast
```

### 13.2 使用dumpsys

```bash
adb shell dumpsys activity broadcasts
```

### 13.3 添加日志

在关键位置添加日志：
- `ActivityManagerService.broadcastIntent()`
- `BroadcastQueue.processNextBroadcast()`
- `deliverToRegisteredReceiverLocked()`

## 总结

广播发送流程是一个复杂的系统级操作，涉及：
1. **应用层**：调用发送接口
2. **系统服务层**：AMS处理和管理
3. **队列管理**：BroadcastQueue调度
4. **进程间通信**：Binder IPC
5. **接收者处理**：BroadcastReceiver.onReceive()

理解这个流程有助于：
- 优化广播性能
- 解决广播相关问题
- 设计合理的广播机制
