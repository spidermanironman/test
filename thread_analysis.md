# Android 进程线程状态分析

## 1. 线程状态分布统计

| 状态 | 数量 | 说明 |
|------|------|------|
| **futex_wait** | ~180 | 等待互斥锁/条件变量，线程处于休眠状态 |
| **do_epoll_wait** | ~95 | 等待I/O事件，事件循环线程 |
| **binder_thread_read** | 15 | 等待Binder IPC消息 |
| **__arm64_sys_ppoll** | 15 | 等待文件描述符事件 |
| **0 (Running/Runnable)** | 13 | **可能正在执行代码** |
| **pipe_read** | 1 | 等待管道读取 |
| **wait_woken** | 1 | 等待被唤醒 |
| **do_sigtimedwait** | 1 | 等待信号 |
| **unix_stream_read_generic** | 1 | 读取Unix socket |
| **folio_wait_bit_common** | 1 | **等待内存页面I/O** ⚠️ |

## 2. 关键线程分析

### 🔴 最可疑 - Native Alloc 相关线程

#### sysTid=21953 - `folio_wait_bit_common`
**这是最可能在进行 native allocation 的线程！**

- `folio_wait_bit_common` 是 Linux 内核中与内存页面管理相关的函数
- 当线程需要分配内存，但需要等待物理页面准备好时会进入此状态
- 可能场景：
  - 大量内存分配导致系统需要回收/交换页面
  - mmap 新内存区域
  - 等待页面从磁盘加载

### 🟡 正在运行的线程 (状态=0)

以下线程正在 CPU 上执行或等待执行，可能正在进行 native allocation：

| sysTid | 可能的角色 |
|--------|-----------|
| 19816 | 早期启动线程 |
| 19818 | 早期启动线程 |
| 19916 | 可能是工作线程 |
| 19974 | 可能是工作线程 |
| 20142 | 可能是工作线程 |
| 20152 | 可能是工作线程 |
| 20291 | 可能是工作线程 |
| 20383 | 可能是工作线程 |
| 23929 | 后期创建的线程 |
| 24883 | 后期创建的线程 |
| 10323 | 低TID，可能是系统服务线程 |
| 11170 | 低TID，可能是系统服务线程 |
| 11794 | 低TID，可能是系统服务线程 |

## 3. 整体状态评估

### 进程状态：**大部分线程空闲，少数线程活跃**

- **总线程数**: 约 320+ 个线程
- **活跃线程**: 约 13 个 (状态=0)
- **等待同步**: 约 180 个 (futex_wait) - 表明有大量线程池
- **等待I/O**: 约 95 个 (epoll_wait) - 正常的事件驱动架构

### 内存分配相关观察

1. **sysTid=21953** (`folio_wait_bit_common`) 是**最明确的内存分配等待**
   - 这个线程正在等待内存页面操作完成
   - 可能正在进行大内存块分配或 mmap

2. 状态为 "0" 的线程中，任何一个都可能正在进行 native allocation
   - 需要结合堆栈信息进一步确认

## 4. 建议的进一步排查

1. **获取 sysTid=21953 的完整堆栈**
   ```bash
   cat /proc/<pid>/task/21953/stack
   ```

2. **检查状态为 0 的线程堆栈**
   - 特别关注 19816, 19818, 20142, 20152, 20291, 20383

3. **使用 simpleperf 或 perfetto 抓取**
   ```bash
   simpleperf record -p <pid> -g --duration 5
   ```

4. **检查 /proc/<pid>/maps 和 /proc/<pid>/smaps**
   - 查看内存映射区域变化

## 5. 总结

| 优先级 | 线程 | 原因 |
|--------|------|------|
| 🔴 高 | **21953** | folio_wait_bit_common 明确表示在等待内存页面 |
| 🟡 中 | 20142, 20152, 20291, 20383 | 运行状态，TID 在主进程范围内 |
| 🟢 低 | 其他状态=0的线程 | 可能是系统线程或不相关 |
