# AOSP 内核 Direct Reclaim 耗时日志添加 - 完整修改指南

## 一、修改目标
在应用线程执行 `mm_vmscan_direct_reclaim` 操作完成时，打印操作耗时日志。

## 二、需要修改的文件

### 唯一修改文件：`mm/vmscan.c`

**完整路径：** `kernel/common/mm/vmscan.c`

---

## 三、具体修改位置和代码

### 修改点 1：添加必要的头文件

**位置：** `mm/vmscan.c` 文件头部，在现有 `#include` 语句之后

**添加内容：**
```c
#include <linux/ktime.h>
#include <linux/sched.h>
#include <linux/sched/task.h>
```

**说明：** 
- `ktime.h` - 提供时间相关函数（ktime_get, ktime_to_us 等）
- `sched.h` 和 `sched/task.h` - 提供进程信息（current->pid, current->comm）

---

### 修改点 2：在函数开始处添加时间记录

**位置：** `mm_vmscan_direct_reclaim` 函数内部，第一行代码之后

**函数签名（参考）：**
```c
unsigned long mm_vmscan_direct_reclaim(gfp_t gfp_mask, int order,
				       struct zonelist *zonelist,
				       nodemask_t *nodemask,
				       int priority, struct scan_control *sc)
```

**在函数开始处添加：**
```c
unsigned long mm_vmscan_direct_reclaim(gfp_t gfp_mask, int order,
				       struct zonelist *zonelist,
				       nodemask_t *nodemask,
				       int priority, struct scan_control *sc)
{
	unsigned long nr_reclaimed = 0;
	
	// ========== 添加以下代码 ==========
	ktime_t start_time, end_time;
	s64 duration_us;
	unsigned long duration_ms;
	
	/* 记录 direct reclaim 操作开始时间 */
	start_time = ktime_get();
	// ========== 添加结束 ==========
	
	// ... 原有的 direct reclaim 逻辑保持不变 ...
```

---

### 修改点 3：在函数返回前添加耗时计算和日志打印

**位置：** `mm_vmscan_direct_reclaim` 函数内部，`return nr_reclaimed;` 语句之前

**添加内容：**
```c
	// ... 原有的 direct reclaim 逻辑 ...
	
	// ========== 添加以下代码 ==========
	/* 计算耗时并打印日志 */
	end_time = ktime_get();
	duration_us = ktime_to_us(ktime_sub(end_time, start_time));
	duration_ms = (unsigned long)(duration_us / 1000);
	
	/* 打印耗时日志 */
	if (duration_ms > 0 || duration_us > 0) {
		pr_info("direct_reclaim: pid=%d comm=%s duration=%lu ms (%lld us) order=%d priority=%d reclaimed=%lu pages gfp_mask=0x%x\n",
			current->pid,
			current->comm,
			duration_ms,
			duration_us,
			order,
			priority,
			nr_reclaimed,
			gfp_mask);
	}
	// ========== 添加结束 ==========
	
	return nr_reclaimed;
}
```

---

## 四、完整修改后的函数示例

```c
unsigned long mm_vmscan_direct_reclaim(gfp_t gfp_mask, int order,
				       struct zonelist *zonelist,
				       nodemask_t *nodemask,
				       int priority, struct scan_control *sc)
{
	unsigned long nr_reclaimed = 0;
	ktime_t start_time, end_time;
	s64 duration_us;
	unsigned long duration_ms;
	
	/* 记录 direct reclaim 操作开始时间 */
	start_time = ktime_get();
	
	/*
	 * 原有的 direct reclaim 逻辑
	 * 这部分代码保持不变，例如：
	 * - 设置 scan_control 参数
	 * - 调用 do_try_to_free_pages 等
	 * - 更新 nr_reclaimed
	 */
	
	/* 计算耗时并打印日志 */
	end_time = ktime_get();
	duration_us = ktime_to_us(ktime_sub(end_time, start_time));
	duration_ms = (unsigned long)(duration_us / 1000);
	
	if (duration_ms > 0 || duration_us > 0) {
		pr_info("direct_reclaim: pid=%d comm=%s duration=%lu ms (%lld us) order=%d priority=%d reclaimed=%lu pages gfp_mask=0x%x\n",
			current->pid,
			current->comm,
			duration_ms,
			duration_us,
			order,
			priority,
			nr_reclaimed,
			gfp_mask);
	}
	
	return nr_reclaimed;
}
```

---

## 五、修改点汇总表

| 序号 | 文件路径 | 修改位置 | 行号范围（参考） | 修改内容 |
|------|----------|----------|------------------|----------|
| 1 | `kernel/common/mm/vmscan.c` | 文件头部 | 约第 20-40 行 | 添加 3 个头文件 |
| 2 | `kernel/common/mm/vmscan.c` | `mm_vmscan_direct_reclaim` 函数开始 | 函数第一行后 | 添加时间变量和开始时间记录 |
| 3 | `kernel/common/mm/vmscan.c` | `mm_vmscan_direct_reclaim` 函数返回前 | `return` 语句前 | 添加耗时计算和日志打印 |

---

## 六、编译和验证步骤

### 1. 应用修改
```bash
cd kernel/common
# 编辑 mm/vmscan.c 文件，按照上述修改点进行修改
```

### 2. 编译内核
```bash
# 配置内核（如果需要）
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-android- menuconfig

# 编译内核
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-android-
```

### 3. 刷入设备并查看日志
```bash
# 刷入内核到设备
adb reboot bootloader
fastboot flash boot boot.img

# 查看日志
adb shell dmesg | grep direct_reclaim

# 或者实时监控
adb shell dmesg -w | grep direct_reclaim
```

### 4. 预期日志输出示例
```
[  123.456789] direct_reclaim: pid=1234 comm=com.example.app duration=5 ms (5234 us) order=0 priority=12 reclaimed=10 pages gfp_mask=0x40d0
[  125.789012] direct_reclaim: pid=5678 comm=system_server duration=2 ms (2345 us) order=1 priority=10 reclaimed=5 pages gfp_mask=0x40d0
```

---

## 七、日志字段说明

| 字段 | 说明 |
|------|------|
| `pid` | 进程 ID，标识是哪个进程触发的 direct reclaim |
| `comm` | 进程名称（comm），显示进程的可执行文件名 |
| `duration` | 耗时，以毫秒（ms）和微秒（us）显示 |
| `order` | 分配阶数，表示需要分配的内存块大小（2^order 页） |
| `priority` | 优先级，数值越小优先级越高 |
| `reclaimed` | 回收的页数 |
| `gfp_mask` | 分配标志位，表示分配内存时的约束条件 |

---

## 八、注意事项

1. **函数位置确认**：不同内核版本的 `mm_vmscan_direct_reclaim` 函数位置可能略有不同，请先搜索确认函数位置
2. **头文件检查**：某些内核版本可能已经包含了部分头文件，添加前先检查避免重复
3. **日志级别**：
   - `pr_info` - 会输出到 dmesg，适合生产环境
   - `pr_debug` - 需要启用 `CONFIG_DYNAMIC_DEBUG`，适合调试
4. **性能影响**：时间记录和日志打印有轻微开销，在高频调用场景下需注意
5. **编译错误处理**：如果编译出错，检查：
   - 头文件是否正确包含
   - 变量类型是否匹配
   - 函数签名是否与内核版本一致

---

## 九、故障排查

### 问题 1：编译错误 "undefined reference to ktime_get"
**解决**：确保包含了 `#include <linux/ktime.h>`

### 问题 2：编译错误 "current undeclared"
**解决**：确保包含了 `#include <linux/sched.h>`

### 问题 3：日志没有输出
**解决**：
- 检查 `CONFIG_PRINTK` 是否启用
- 确认日志级别设置正确
- 使用 `dmesg -w` 实时监控

### 问题 4：日志输出过多
**解决**：
- 将 `pr_info` 改为 `pr_debug`
- 添加条件过滤，例如只记录耗时超过某个阈值的操作

---

## 十、高级选项（可选）

### 选项 A：只记录超过阈值的操作
```c
#define DIRECT_RECLAIM_LOG_THRESHOLD_US 1000  // 1ms

if (duration_us > DIRECT_RECLAIM_LOG_THRESHOLD_US) {
	pr_info("direct_reclaim: ...");
}
```

### 选项 B：使用 tracepoint（更专业的性能追踪）
参考 `DIRECT_RECLAIM_LOG_MODIFICATION.md` 中的 tracepoint 方案

### 选项 C：添加统计信息
可以维护一个全局统计结构，记录 direct reclaim 的总次数、总耗时等

---

## 总结

**核心修改：**
1. 在 `mm/vmscan.c` 文件头部添加 3 个头文件
2. 在 `mm_vmscan_direct_reclaim` 函数开始处记录开始时间
3. 在函数返回前计算耗时并打印日志

**修改文件数量：** 1 个文件（`mm/vmscan.c`）
**修改位置数量：** 3 处（头文件 + 函数开始 + 函数返回前）
