# 如何检查代码是否合入某个版本

本文档介绍几种常用的方法来检查代码是否已经合并到某个版本（分支或标签）中。

## 方法一：检查提交是否在某个分支中

### 1.1 使用 `git branch --contains`

检查某个提交（commit）是否在指定分支中：

```bash
# 检查提交是否在 main 分支中
git branch --contains <commit-hash>

# 检查提交是否在远程分支中
git branch -a --contains <commit-hash>

# 示例：检查提交 26c2364 在哪些分支中
git branch -a --contains 26c2364
```

### 1.2 检查当前分支是否包含某个提交

```bash
# 检查当前分支是否包含某个提交
git branch --contains <commit-hash> | grep '*'

# 或者直接检查
git log --oneline | grep <commit-hash>
```

## 方法二：检查提交是否在某个标签（版本）中

### 2.1 使用 `git tag --contains`

```bash
# 列出包含某个提交的所有标签
git tag --contains <commit-hash>

# 检查特定标签是否包含某个提交
git tag --contains <commit-hash> | grep <tag-name>
```

### 2.2 查看标签的提交历史

```bash
# 查看某个标签的提交历史
git log <tag-name> --oneline

# 检查某个提交是否在标签中
git log <tag-name> --oneline | grep <commit-hash>
```

## 方法三：使用 `git cherry` 检查未合并的提交

```bash
# 检查当前分支相对于另一个分支有哪些提交未合并
git cherry <upstream-branch> <your-branch>

# 示例：检查 main 分支相对于 origin/main 的未合并提交
git cherry origin/main main
```

## 方法四：使用 `git log` 查找提交

### 4.1 在特定分支中查找提交

```bash
# 在 main 分支中查找提交
git log main --oneline | grep <commit-hash>

# 在特定标签中查找提交
git log <tag-name> --oneline | grep <commit-hash>
```

### 4.2 使用 `--all` 在所有分支中查找

```bash
# 在所有分支中查找包含某个提交的分支
git log --all --oneline | grep <commit-hash>

# 查看某个提交在哪些分支中
git branch -a --contains <commit-hash>
```

## 方法五：检查文件是否在某个版本中

```bash
# 检查某个文件在特定分支/标签中的版本
git show <branch-or-tag>:<file-path>

# 查看文件在某个版本中的内容
git show <tag-name>:<file-path>

# 比较文件在不同版本中的差异
git diff <tag1> <tag2> -- <file-path>
```

## 方法六：使用 `git merge-base` 检查合并关系

```bash
# 查找两个分支的共同祖先
git merge-base <branch1> <branch2>

# 检查某个提交是否在两个分支的共同历史中
git merge-base --is-ancestor <commit-hash> <branch-name>
```

## 实用示例脚本

创建一个检查脚本：

```bash
#!/bin/bash
# check_commit_in_version.sh

COMMIT_HASH=$1
VERSION=$2  # 可以是分支名或标签名

if [ -z "$COMMIT_HASH" ] || [ -z "$VERSION" ]; then
    echo "用法: $0 <commit-hash> <branch-or-tag>"
    exit 1
fi

echo "检查提交 $COMMIT_HASH 是否在 $VERSION 中..."
echo ""

# 检查是否在分支中
if git branch -a | grep -q "$VERSION"; then
    if git branch -a --contains "$COMMIT_HASH" | grep -q "$VERSION"; then
        echo "✓ 提交 $COMMIT_HASH 在分支 $VERSION 中"
    else
        echo "✗ 提交 $COMMIT_HASH 不在分支 $VERSION 中"
    fi
fi

# 检查是否在标签中
if git tag | grep -q "^$VERSION$"; then
    if git tag --contains "$COMMIT_HASH" | grep -q "^$VERSION$"; then
        echo "✓ 提交 $COMMIT_HASH 在标签 $VERSION 中"
    else
        echo "✗ 提交 $COMMIT_HASH 不在标签 $VERSION 中"
    fi
fi

# 显示提交信息
echo ""
echo "提交信息:"
git log -1 "$COMMIT_HASH" --oneline
```

## 快速检查命令总结

```bash
# 1. 检查提交在哪些分支中
git branch -a --contains <commit-hash>

# 2. 检查提交在哪些标签中
git tag --contains <commit-hash>

# 3. 检查提交是否在特定分支中
git log <branch-name> --oneline | grep <commit-hash>

# 4. 检查提交是否在特定标签中
git log <tag-name> --oneline | grep <commit-hash>

# 5. 查看提交的详细信息
git show <commit-hash>

# 6. 查看提交的合并历史
git log --graph --oneline --all | grep <commit-hash>
```

## 注意事项

1. **提交哈希**：可以使用完整的 commit hash 或短 hash（前7-8位）
2. **远程分支**：检查远程分支时，确保先执行 `git fetch` 获取最新信息
3. **标签同步**：如果标签在远程，需要先执行 `git fetch --tags`
4. **合并提交**：如果提交是通过 merge 合并的，可能需要检查 merge commit 的父提交
