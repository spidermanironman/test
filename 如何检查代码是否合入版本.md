# 如何检查代码是否合入某个版本

本文档介绍几种常用的方法来检查代码是否已经合并到某个版本（分支或标签）。

## 方法一：检查提交是否在某个分支中

### 1. 查看包含某个提交的所有分支
```bash
# 查看包含指定提交的所有分支
git branch --contains <commit-hash>

# 查看包含指定提交的所有远程分支
git branch -r --contains <commit-hash>

# 查看包含指定提交的所有分支（本地+远程）
git branch -a --contains <commit-hash>
```

**示例：**
```bash
# 假设提交hash是 abc1234
git branch --contains abc1234
# 输出会显示所有包含该提交的分支
```

### 2. 检查提交是否在特定分支中
```bash
# 检查提交是否在main分支中
git branch --contains <commit-hash> | grep main

# 或者直接切换到目标分支查看
git checkout <branch-name>
git log --oneline | grep <commit-hash>
```

## 方法二：检查提交是否在某个标签中

### 1. 查看包含某个提交的所有标签
```bash
# 查看包含指定提交的所有标签
git tag --contains <commit-hash>
```

**示例：**
```bash
git tag --contains abc1234
# 输出会显示所有包含该提交的标签（版本）
```

### 2. 检查提交是否在特定标签中
```bash
# 检查提交是否在v1.0.0标签中
git tag --contains <commit-hash> | grep v1.0.0

# 或者查看标签对应的提交
git log <tag-name> --oneline | grep <commit-hash>
```

## 方法三：使用 git log 在特定版本中搜索

### 1. 在特定分支中搜索提交
```bash
# 在main分支中搜索包含特定提交
git log <branch-name> --oneline | grep <commit-hash>

# 或者查看完整的提交信息
git log <branch-name> | grep -A 5 -B 5 <commit-hash>
```

### 2. 在特定标签中搜索提交
```bash
# 在v1.0.0标签中搜索提交
git log <tag-name> --oneline | grep <commit-hash>
```

## 方法四：使用 git merge-base 和 git cherry

### 1. 使用 git cherry 比较分支差异
```bash
# 查看当前分支相对于目标分支的提交
git cherry <target-branch> <current-branch>

# 查看哪些提交已经在目标分支中（带+号表示已存在）
git cherry -v <target-branch> <current-branch>
```

### 2. 使用 git merge-base 查找共同祖先
```bash
# 查找两个分支的共同祖先
git merge-base <branch1> <branch2>

# 如果提交在共同祖先之后，说明可能已经合并
git log <merge-base>..<commit-hash>
```

## 方法五：使用 git show-branch 可视化

```bash
# 可视化显示分支关系
git show-branch <branch1> <branch2> | grep <commit-hash>
```

## 方法六：检查文件是否在某个版本中

### 1. 检查文件是否存在于某个分支
```bash
# 检查文件是否在main分支中
git ls-tree -r <branch-name> --name-only | grep <file-path>

# 或者直接查看文件内容
git show <branch-name>:<file-path>
```

### 2. 检查文件是否存在于某个标签
```bash
# 检查文件是否在v1.0.0标签中
git show <tag-name>:<file-path>
```

## 实用脚本示例

### 脚本1：检查提交是否在多个版本中
```bash
#!/bin/bash
COMMIT_HASH=$1

echo "检查提交 $COMMIT_HASH 是否在以下版本中："
echo "=========================================="

# 检查main分支
if git branch --contains $COMMIT_HASH | grep -q "main"; then
    echo "✓ 已在 main 分支中"
else
    echo "✗ 不在 main 分支中"
fi

# 检查所有标签
TAGS=$(git tag --contains $COMMIT_HASH)
if [ -n "$TAGS" ]; then
    echo "✓ 已在以下标签中："
    echo "$TAGS"
else
    echo "✗ 不在任何标签中"
fi
```

### 脚本2：检查当前分支的提交是否已合入目标分支
```bash
#!/bin/bash
TARGET_BRANCH=$1  # 例如: main
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)

echo "检查 $CURRENT_BRANCH 的提交是否已合入 $TARGET_BRANCH"
echo "=========================================="

# 获取当前分支独有的提交
UNMERGED=$(git cherry $TARGET_BRANCH $CURRENT_BRANCH | grep "^+" | wc -l)

if [ $UNMERGED -eq 0 ]; then
    echo "✓ 所有提交都已合入 $TARGET_BRANCH"
else
    echo "✗ 还有 $UNMERGED 个提交未合入 $TARGET_BRANCH"
    echo "未合入的提交："
    git cherry $TARGET_BRANCH $CURRENT_BRANCH | grep "^+"
fi
```

## 快速参考命令

| 需求 | 命令 |
|------|------|
| 查看包含提交的所有分支 | `git branch -a --contains <commit>` |
| 查看包含提交的所有标签 | `git tag --contains <commit>` |
| 检查提交是否在main分支 | `git branch --contains <commit> \| grep main` |
| 比较两个分支的差异 | `git cherry <branch1> <branch2>` |
| 在分支中搜索提交 | `git log <branch> --oneline \| grep <commit>` |
| 查看分支关系图 | `git log --graph --oneline --all` |

## 注意事项

1. **提交hash可以是完整hash或短hash**（通常前7-8位即可）
2. **如果提交不存在**，命令会返回错误或空结果
3. **标签通常代表发布版本**，检查标签可以确认代码是否在某个发布版本中
4. **使用 `git fetch` 更新远程分支信息**，确保检查的是最新状态
5. **合并提交（merge commit）** 也会被包含在结果中

## 实际使用示例

假设你想检查提交 `abc1234` 是否在 `main` 分支和 `v1.0.0` 标签中：

```bash
# 1. 检查是否在main分支
git branch --contains abc1234 | grep main

# 2. 检查是否在v1.0.0标签
git tag --contains abc1234 | grep v1.0.0

# 3. 如果都不在，查看提交的详细信息
git show abc1234

# 4. 查看提交在哪个分支中
git branch -a --contains abc1234
```
