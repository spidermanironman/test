# Direct Reclaim 耗时日志添加 - 修改点总结

## 修改文件清单

### 文件 1: `kernel/common/mm/vmscan.c`

#### 修改位置 1: 文件头部（添加头文件）
- **行号范围**: 文件开头 `#include` 区域
- **操作**: 添加以下头文件（如果不存在）
```c
#include <linux/ktime.h>
#include <linux/sched.h>
#include <linux/sched/task.h>
```

#### 修改位置 2: `mm_vmscan_direct_reclaim` 函数开始处
- **函数名**: `mm_vmscan_direct_reclaim`
- **操作**: 在函数开始处添加时间记录变量和开始时间记录
- **插入位置**: 函数第一行 `unsigned long nr_reclaimed = 0;` 之后

**添加代码：**
```c
ktime_t start_time, end_time;
s64 duration_us;
unsigned long duration_ms;

// 记录开始时间
start_time = ktime_get();
```

#### 修改位置 3: `mm_vmscan_direct_reclaim` 函数返回前
- **函数名**: `mm_vmscan_direct_reclaim`
- **操作**: 在 `return nr_reclaimed;` 之前添加耗时计算和日志打印
- **插入位置**: 函数最后，`return` 语句之前

**添加代码：**
```c
// 计算耗时
end_time = ktime_get();
duration_us = ktime_to_us(ktime_sub(end_time, start_time));
duration_ms = (unsigned long)(duration_us / 1000);

// 打印耗时日志
if (duration_ms > 0 || duration_us > 0) {
	pr_info("direct_reclaim: pid=%d comm=%s duration=%lu ms (%lld us) order=%d priority=%d reclaimed=%lu pages gfp_mask=0x%x\n",
		current->pid,
		current->comm,
		duration_ms,
		duration_us,
		order,
		priority,
		nr_reclaimed,
		gfp_mask);
}
```

---

## 完整修改示例

### 修改前：
```c
unsigned long mm_vmscan_direct_reclaim(gfp_t gfp_mask, int order,
				       struct zonelist *zonelist,
				       nodemask_t *nodemask,
				       int priority, struct scan_control *sc)
{
	unsigned long nr_reclaimed = 0;
	
	// ... 原有代码 ...
	
	return nr_reclaimed;
}
```

### 修改后：
```c
unsigned long mm_vmscan_direct_reclaim(gfp_t gfp_mask, int order,
				       struct zonelist *zonelist,
				       nodemask_t *nodemask,
				       int priority, struct scan_control *sc)
{
	unsigned long nr_reclaimed = 0;
	ktime_t start_time, end_time;
	s64 duration_us;
	unsigned long duration_ms;
	
	// 记录开始时间
	start_time = ktime_get();
	
	// ... 原有代码保持不变 ...
	
	// 计算耗时并打印日志
	end_time = ktime_get();
	duration_us = ktime_to_us(ktime_sub(end_time, start_time));
	duration_ms = (unsigned long)(duration_us / 1000);
	
	if (duration_ms > 0 || duration_us > 0) {
		pr_info("direct_reclaim: pid=%d comm=%s duration=%lu ms (%lld us) order=%d priority=%d reclaimed=%lu pages gfp_mask=0x%x\n",
			current->pid,
			current->comm,
			duration_ms,
			duration_us,
			order,
			priority,
			nr_reclaimed,
			gfp_mask);
	}
	
	return nr_reclaimed;
}
```

---

## 修改点汇总表

| 序号 | 文件路径 | 修改位置 | 修改类型 | 说明 |
|------|----------|----------|----------|------|
| 1 | `kernel/common/mm/vmscan.c` | 文件头部 | 添加头文件 | 添加 `ktime.h`, `sched.h` 等 |
| 2 | `kernel/common/mm/vmscan.c` | `mm_vmscan_direct_reclaim` 函数开始 | 添加变量和时间记录 | 在函数开始处记录开始时间 |
| 3 | `kernel/common/mm/vmscan.c` | `mm_vmscan_direct_reclaim` 函数返回前 | 添加耗时计算和日志 | 在 return 前计算耗时并打印 |

---

## 验证方法

修改完成后，编译内核并查看日志：

```bash
# 编译内核
cd kernel/common
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-android-

# 查看日志
adb shell dmesg | grep direct_reclaim
```

预期日志输出格式：
```
direct_reclaim: pid=1234 comm=com.example.app duration=5 ms (5234 us) order=0 priority=12 reclaimed=10 pages gfp_mask=0x40d0
```

---

## 注意事项

1. **函数签名**: 确保 `mm_vmscan_direct_reclaim` 的函数签名与你的内核版本一致
2. **编译检查**: 修改后需要重新编译内核模块
3. **日志级别**: `pr_info` 会输出到 dmesg，如果日志过多可考虑使用 `pr_debug`
4. **性能影响**: 时间记录和日志打印有轻微性能开销，但通常可忽略
